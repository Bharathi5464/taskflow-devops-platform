name: TaskFlow CD Pipeline

# Run CD after CI pipeline finishes
on:
  workflow_run:
    workflows: ["TaskFlow CI Pipeline"]
    types:
      - completed

# Global variables
env:
  AWS_REGION: ap-south-1
  ECR_REPOSITORY: aws-terraform-platform-taskflow

  # Use CI commit SHA as image version
  IMAGE_TAG: ${{ github.event.workflow_run.head_sha }}

jobs:
  deploy:

    # Deploy only when CI succeeds
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest

    steps:

      # Checkout the same commit that passed CI
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha }}

      # Install Helm
      - name: Setup Helm
        uses: azure/setup-helm@v4

      # Configure AWS access
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}
      # Login to ECR
      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2
      # Build Docker image
      - name: Build Docker Image
        run: |
          docker build -t taskflow-app:${{ env.IMAGE_TAG }} .

      # Tag image for ECR
      - name: Tag Docker Image
        run: |
          docker tag taskflow-app:${{ env.IMAGE_TAG }} \
            ${{ steps.login-ecr.outputs.registry }}/${{ env.ECR_REPOSITORY }}:${{ env.IMAGE_TAG }}

      # Push image to ECR
      - name: Push Docker Image
        run: |
          docker push \
            ${{ steps.login-ecr.outputs.registry }}/${{ env.ECR_REPOSITORY }}:${{ env.IMAGE_TAG }}

      # Install SSH client
      - name: Install SSH Client
        run: |
          sudo apt-get update
          sudo apt-get install -y openssh-client

      # Load EC2 SSH key
      - name: Start SSH Agent
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.EC2_SSH_KEY }}

      # Deploy application to Kubernetes
      - name: Deploy Application to Kubernetes
        run: |
          ssh -o StrictHostKeyChecking=no \
            ${{ secrets.EC2_USERNAME }}@${{ secrets.EC2_HOST }} << EOF

            # Login EC2 to ECR
            aws ecr get-login-password --region ${{ env.AWS_REGION }} | \
            docker login \
              --username AWS \
              --password-stdin \
              ${{ steps.login-ecr.outputs.registry }}

            # Install or upgrade Helm release
            helm upgrade --install taskflow ./taskflow-chart \
              --namespace taskflow \
              --create-namespace \
              --set image.repository=${{ steps.login-ecr.outputs.registry }}/${{ env.ECR_REPOSITORY }} \
              --set image.tag=${{ env.IMAGE_TAG }} \
              --set image.pullPolicy=Always \
              --wait \
              --timeout 180s

            # Wait for deployment rollout
            kubectl rollout status deployment/taskflow-taskflow-chart \
              -n taskflow \
              --timeout=180s

          EOF

      # Verify deployment and application health
      - name: Verify Kubernetes Deployment and Health
        run: |
          ssh -o StrictHostKeyChecking=no \
            ${{ secrets.EC2_USERNAME }}@${{ secrets.EC2_HOST }} << 'EOF'

            # Check Pods
            echo "=== Kubernetes Pods ==="
            kubectl get pods -n taskflow -o wide

            # Check application health
            echo "=== Application Health ==="
            kubectl run curl-test \
              -n taskflow \
              --rm -i \
              --restart=Never \
              --image=curlimages/curl \
              -- curl -s --fail http://taskflow-taskflow-chart:5000/health

          EOF